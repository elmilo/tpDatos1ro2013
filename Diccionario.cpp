#include "Diccionario.h"

/*************************************************************************************/
/*************************************************************************************/
bool Diccionario::buscarTermino(string unToken) {

	Termino* desref;

//if ( diccionario.count(desref->getTermino() == unToken) == 1 ) NO COMPILA
	if (true)
		return true;
	return false;

	/*for (elemento = documentos.begin(); elemento != documentos.end(); ++elemento){
	 desref = *elemento;
	 if (desref->getDocID() == unDocID){
	 return elemento;
	 }
	 }

	 return documentos.end();*/
}
;

/*************************************************************************************/
/*************************************************************************************/
void Diccionario::agregar(string unToken, unsigned unaPosicion,
		unsigned unDocID) {
}
;
